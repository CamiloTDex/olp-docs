# /auth/login

Retrieves the user token for a specified username and password combination.

```ejs
throw "Invalid credentials"

/auth/login {
    username: string,
    password: string
} => string
```
